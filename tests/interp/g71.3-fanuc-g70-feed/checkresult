#!/bin/bash
set -e
result=${1:-result}
grep -q 'SET_FEED_RATE(50.0000' "$result" || {
    echo "G70.3 did not pick F50 from the P-Q profile"
    exit 1
}
grep -q STRAIGHT_FEED "$result" || {
    echo "no STRAIGHT_FEED in output"
    exit 1
}
exit 0
